<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="MyAppThemeWithTimePicker" parent="android:Theme.Material" tools:targetApi="21">
        <!-- Customize your theme here. -->
        <item name="android:dialogTheme">@style/MyDialogTheme</item>
        <item name="android:timePickerStyle">@style/MyTimePicker</item>
    </style>

    <style name="MyDialogTheme" parent="android:Theme.Material.Dialog" tools:targetApi="21">
        <item name="android:timePickerStyle">@style/MyTimePicker</item>
    </style>

    <style name="MyTimePicker" parent="android:Widget.Material.TimePicker" tools:targetApi="21">
        <item name="android:timePickerMode">spinner</item>
    </style>

</resources>
